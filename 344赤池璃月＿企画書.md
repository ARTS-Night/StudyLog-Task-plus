# スタログ授業タスク管理 Chrome 拡張機能 企画書

## 1. テーマ

授業で忘れてはいけないことを、忘れないための仕組みを作る。

スタログの授業ごとにタスクを登録し、普段確認する画面の中で提出物や作業を管理できる Chrome 拡張機能を制作する。Chrome 上の表示名は「スタログ授業メモ」とする。

## 2. 背景と課題

私は忘れやすいことがあり、紙や別のメモアプリへ記録しても、後から記録した場所自体が分からなくなることがあった。また、授業の情報はスタログ、やることは別のアプリというように確認場所が分かれると、入力や確認を後回しにしやすい。

そこで、日常的に開くスタログへタスク管理機能を直接追加し、授業情報とやることを同じ場所で確認できるようにする。

## 3. 目的

- 授業ごとの提出物や作業を、その授業の画面からすぐに登録できるようにする。
- 登録したタスクを、授業画面と全体一覧のどちらからでも確認できるようにする。
- 別のメモアプリを探したり開いたりする手間を減らす。
- 見ただけで操作方法が分かる、日常的に使いやすい画面を目指す。

## 4. 仮説

スタログのカレンダーや授業詳細へ授業別のタスク機能を追加すれば、別のアプリを開く手間が減り、提出物や作業を確認しやすくなる。その結果、確認漏れを減らせる可能性がある。

## 5. 対象と利用環境

- 対象：岩崎学園のスタログを利用する学生
- 対応環境：デスクトップ版 Google Chrome
- 対象サイト：岩崎学園ポータル内のスタログ
- 利用条件：スタログへログインできること

本作品はスタログの公式機能ではなく、個人制作の Chrome 拡張機能である。スマートフォン版 Chrome や他のブラウザには対応していない。

## 6. 成果物

Manifest V3 に対応した Chrome 拡張機能「スタログ授業メモ」を制作する。HTML、CSS、JavaScript と Chrome Extension API のみを使用し、ビルド処理や外部ライブラリを必要としない構成にする。

### 主な機能

| 機能 | 内容 |
| --- | --- |
| 授業別タスク | カレンダーと授業詳細から、タスクの追加・編集・完了・削除を行う |
| 全体一覧 | ツールバーのアイコンや専用画面から、全授業のタスクを確認する |
| 授業選択 | スタログのマイページから年度別の授業名と授業 ID を取得し、IDを知らなくても追加先を選べるようにする |
| 検索・絞り込み | タスク本文・授業名による検索と、未完了・完了の状態絞り込みを行う |
| 日付記録 | タスクの追加日時と完了日時を記録し、月日を画面へ表示する |
| 自動整理 | 完了から指定日数が過ぎたタスクを、次回利用時に自動削除する。初期設定では削除しない |
| 端末間同期 | `chrome.storage.sync` を使用し、同じ Google アカウントで Chrome 同期が有効なパソコン間で共有する |
| ローカル保存 | 同期を使わず、この端末だけへ保存する設定も用意する |
| Tree Ivy 対応 | Tree Ivy Replanted の右サイドに表示された授業ページでもタスクを操作できるようにする |
| バックアップ | 設定画面から JSON 形式でエクスポート・インポートできるようにする |

## 7. データ保存とプライバシー

タスクは Chrome Extension Storage に保存する。同期モードでは `chrome.storage.sync`、この端末だけで使う場合は `chrome.storage.local` を利用する。Google APIや独自サーバーへ直接送信する機能は設けない。

授業一覧のために保存する情報は、年度・授業 ID・科目名に限定する。マイページの HTML 全体や氏名は保存しない。Chrome Sync を利用するには、同じ Google アカウントで Chrome にログインし、拡張機能の同期を有効にする必要がある。

## 8. 開発方法と方針変更

1. 保存したスタログの HTML を調査し、授業リンクや授業詳細を安定して見つける方法を確認する。
2. 最初は授業ごとの自由記述メモとして試作する。
3. 完了・未完了を区別できる方が用途に合うと判断し、ToDo リスト形式へ変更する。
4. ツールバーの全体一覧、年度別の専用画面、検索、同期、日付記録などを順に追加する。
5. 利用者の感想とテスト結果を基に、表示やデータ保存の問題を修正する。

初期記録では、アイデア整理に30分、初期のコーディングに3時間を使った。その後は機能追加と検証を継続しているため、これは現在の完成版全体の作業時間ではない。

アイコンは Material Symbols のデザインを SVG として同梱し、外部から画像を読み込まなくても機能を判別しやすい見た目にする。

## 9. 検証方法

### 機能の確認

- タスクの追加・編集・完了・未完了化・削除が行えるか。
- 同じ授業が複数の日や時限に表示されても、すべてのタスク表示がページ全体の再読み込みなしで更新されるか。
- 保存先モードを切り替えても、ページ全体を再読み込みせずタスク部分だけが新しい保存先で読み直されるか。
- 検索、状態絞り込み、追加日・完了日の表示、自動削除が正しく動くか。
- 通常の授業詳細と Tree Ivy の右サイド表示の両方で操作できるか。
- 同期保存とローカル保存の両方で、既存タスクを失わずに操作できるか。

### 使いやすさの確認

実際に利用してもらい、次の点を聞き取る。

- 初めて見ても操作方法が分かるか。
- タスクの追加と確認を手軽に行えるか。
- 別のメモアプリを使う場合と比べて便利か。
- 日常的に使いたいと思えるか。

## 10. 試用時に得られた意見

試用協力者2名（学生1名・教員1名）から得た感想を、意味を変えない範囲で要約した。

- 学生：Notion など別のメモアプリへ書き込まず、スタログ内で入力できる点が使いやすい。操作も分かりやすく、日常的に使えそうだと感じた。
- 教員：最初はあまり使わないと思っていたが、実際には手軽にタスクを追加でき、短時間で確認する用途に適していた。

操作の手軽さには肯定的な意見が得られた。一方、試用者が2名と少なく、忘れ物や提出忘れが実際に減ったかを長期間測定したわけではないため、仮説の効果については今後も検証が必要である。

## 11. 今後の課題

- 提出期限を記録し、期限が近いタスクを分かりやすく表示する。
- 任意の並び替え機能を追加する。
- タグは選択肢を増やし過ぎないよう、必要性と使いやすさを確認してから検討する。
- より多くの利用者に一定期間使ってもらい、操作性だけでなく確認漏れが減ったかを調べる。
- 実際の Chrome、複数パソコン間の同期、スタログの DOM 変更を含む実機テストを増やす。
- スマートフォンや他ブラウザへ対応する場合は、別の Web システムが必要になる。認証、個人情報の保護、運用費、学校側の許可を整理した上で検討する。

追加日時・完了日時、検索・状態絞り込み、Tree Ivy の右サイド対応、完了タスクの自動削除は実装済みであり、今後の課題には含めない。
