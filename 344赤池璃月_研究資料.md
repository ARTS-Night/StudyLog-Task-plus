# スタログ授業タスク管理 Chrome 拡張機能 研究資料

対象バージョン：1.2.0

## 1. 研究テーマ

普段利用する学習システムの中へ授業別のタスク管理機能を組み込むことで、入力と確認の負担を減らし、日常的に使いやすい仕組みを作れるかを調べる。

## 2. 研究の背景と目的

提出物や授業中に指示された作業を別のメモアプリや紙へ記録すると、授業情報とタスクの確認場所が分かれる。記録しても、その記録場所を思い出せないこともある。

そこで、学生が普段確認するスタログのカレンダーと授業詳細へ、授業ごとのタスク管理機能を追加した。目的は、別のアプリを開かなくてもタスクを登録・確認できる環境を作り、その使いやすさと技術上の課題を明らかにすることである。

## 3. 仮説

スタログの授業情報と同じ画面でタスクを管理できれば、入力と確認に必要な操作が少なくなる。そのため、利用者は機能を直感的に理解し、日常的に使いやすいと感じると考えた。

ただし、「使いやすいこと」と「忘れ物や提出忘れが実際に減ること」は別の評価である。今回の短期間・少人数の試用では主に操作性を確認し、忘れ物の減少は今後の継続調査項目とする。

## 4. 制作したシステム

制作物は、Manifest V3 の Chrome 拡張機能「スタログ授業メモ」である。スタログへ次の機能を追加した。

- カレンダーと授業詳細での、授業別タスクの追加・編集・完了・削除
- ツールバーと専用画面での全タスク一覧
- マイページから取得した年度別授業一覧による追加先の選択
- タスク本文・授業名の検索と、未完了・完了の状態絞り込み
- タスクの追加日時と完了日時の記録
- 完了後1・3・7・14・30・90日から選べる自動削除（初期設定は無効）
- Chrome Syncによるパソコン間の共有と、この端末だけに保存するモード
- JSONによるバックアップと復元
- Tree Ivy Replanted の右サイド表示への対応

## 5. 技術的な工夫

### 5.1 授業を自動で見つける方法

利用者が授業 ID を入力しなくてもよいように、スタログのホームとマイページから年度・授業 ID・科目名を取得する。ホームには主に現在年度の授業しか表示されないため、マイページから取得した完全な一覧と、ホームから取得した補助一覧を分けて保存する設計にした。

スタログは画面の一部を動的に更新するため、単純にページ読込時だけ処理するとボタンが消えたり重複したりする。そこで DOM の変更を監視し、同じ場所へボタンを二重に追加しないようにした。

### 5.2 Tree Ivyとの互換性

Tree Ivy Replanted は授業詳細を右サイドの iframe 内に表示する。タスクUIを各フレームへ読み込むことで、通常表示と右サイド表示の両方に同じ「マイタスク」を表示できるようにした。一方、授業一覧の取得処理まで各フレームで繰り返さないよう、処理を分離した。

### 5.3 データの保存と同期

タスクは、授業 ID をキーとして Chrome Extension Storage へ保存する。通常は `chrome.storage.sync` を利用し、同じ Google アカウントで Chrome 同期が有効なパソコン間へ反映する。同期を使わない場合は `chrome.storage.local` へ切り替えられる。

これは独自サーバーや Google API へ直接送信する仕組みではない。スマートフォン版 Chrome では拡張機能を利用できないため、現在の同期機能は主に複数のパソコン間を対象とする。

Chromeには「同期データの受信が完全に終わった」と直接判定するAPIがない。そのため、初回起動ではデータ到着または変更通知を待ち、最大20秒の待機を設けた。専用画面で待機中に追加されたタスクは端末内へ一時保存し、同期確認後に重複しないよう反映する。

### 5.4 同時更新への対策

授業詳細、専用画面、ツールバー、別のiframeから同時に操作される可能性がある。古い画面の内容で新しいデータを上書きしないよう、保存直前に対象授業の最新値を読み直し、共通の変更キューで書き込みを順番に処理する。

同じ授業が時間割上で複数の日に続く場合は、1か所で変更した直後に同じ授業 ID のボタンをすべて更新する。ページ全体の再読み込みを行わないため、見ていた位置を保ったまま結果を確認できる。

### 5.5 日付と完了後の整理

新規タスクには追加日時、未完了から完了へ切り替えたときには完了日時を保存する。未完了へ戻した場合は完了日時を削除し、再び完了したときに新しい日時を記録する。

過去のデータに存在しない日時を現在時刻で補うと、事実と異なる記録になる。そのため、日時のない旧タスクには推測した日付を付けない。自動削除も、完了状態で有効な完了日時があるタスクだけを対象にする。

自動削除は指定時刻に常駐して動くのではなく、期限が過ぎた後にスタログや拡張画面を次に開いたときに実行する。この違いが利用者に伝わるよう、専用画面の設定欄にも説明を表示した。

## 6. 検証方法

### 6.1 利用者による試用

試用協力者2名（学生1名・教員1名）に実際の画面を操作してもらい、入力のしやすさ、確認のしやすさ、日常的に利用できそうかについて感想を得た。今回記録できているのは自由回答による感想であり、利用期間や操作回数を統一した比較実験ではない。また、この感想だけでは、後から追加した検索・日時記録・自動削除などの機能まで評価されたとは判断しない。

### 6.2 自動テスト

実スタログのデータを変更せず繰り返し確認できるよう、偽の DOM と Chrome API を使ったテストを用意した。

| テスト | 主な確認内容 |
| --- | --- |
| `tasks-smoke.test.js` | 初回同期待ち、保留タスク、検索UI、追加・完了日時、旧形式データ |
| `manifest-frames.test.js` | Tree Ivy対応に必要なiframe設定とスクリプトの読込順 |
| `content-buttons-smoke.test.js` | 同じ授業 ID の複数ボタンが同時に更新されること |
| `popup-smoke.test.js` | ツールバーポップアップの同期制限、外部変更、完了切替 |
| `mutation-lock.test.js` | 同時変更を順番に処理し、異常終了後も解放されること |
| `sync-guard.test.js` | 初回同期通知、空データ時の待機、ローカル保存モード |
| `task-lifecycle.test.js` | 完了日時、自動削除の期限境界、同時更新、旧形式データの保護 |

これら7本のテストと全JavaScriptファイルの構文検査は、2026年7月18日時点で成功した。ただし、これは実際の Chrome、スタログ、Chrome Sync を使うE2Eテストではない。

## 7. 結果

### 7.1 利用者の感想

自由回答の内容を、意味を変えない範囲で要約した。

- 学生：Notion など別のメモアプリへ書き込まず、スタログ内で入力できる点が使いやすい。操作も分かりやすく、日常的に使えそうだと感じた。
- 教員：最初はあまり使わないと思っていたが、実際には手軽にタスクを追加でき、短時間で確認する用途に適していた。

2名とも、スタログ内で入力と確認が完結する手軽さを肯定的に評価した。

### 7.2 機能検証の結果

自動テストでは、タスクの基本操作、検索、日時保持、同じ授業の複数表示、Tree Ivy用の設定、同期開始時の保護、同時更新、自動削除の境界条件を確認できた。特に、旧形式のタスクへ不正確な日時を付けないことと、別画面で追加された最新データを古い画面が消さないことを重視した。

## 8. 考察

### 8.1 確認場所を増やさないこと

利用者の感想から、機能の数を増やすことだけでなく、普段見るスタログの中で操作を完結できることに価値があると考えられる。専用アプリを新しく覚える必要がなく、授業を見た流れでタスクを追加できる点が、手軽さにつながった。

### 8.2 視覚的な分かりやすさ

チェックボックスや共通のアイコンを使い、完了状態を見た目で判断できるようにした。専用画面では、「追加先の授業を探す検索」と「保存済みタスクを探す検索」を分けた。検索という同じ言葉でも目的が異なるため、ラベルと表示場所を分けることが操作の理解に重要だった。

### 8.3 同期機能の意味と限界

Chrome Syncにより、独自サーバーを運用せず複数のパソコンでタスクを共有できる。一方、同じGoogleアカウント、Chromeへのログイン、拡張機能の同期設定が必要であり、反映時間を拡張機能側から保証することはできない。したがって、画面では「同期済み」と断定せず、同期条件と確認方法を説明する必要がある。

### 8.4 機能を増やし過ぎないこと

タグや細かな分類は便利になる可能性があるが、選択項目が増えると入力が面倒になり、手軽さを失う可能性もある。今後追加する場合は、最初から多数の選択肢を出すのではなく、必要な人だけが使える任意機能として試し、利用状況を確認したい。

## 9. 研究上・実装上の限界

- 試用者は2名であり、年齢や利用目的の異なる多数の利用者を比較していない。
- 利用期間、タスク登録数、提出忘れの件数を記録していないため、忘れ物が減ったとは結論づけられない。
- 自動テストは偽DOM・偽Chrome API上の確認で、実環境のすべての動作を保証するものではない。
- デスクトップ版Chrome専用で、スマートフォンや他ブラウザでは利用できない。
- スタログのDOM構造が変更された場合、授業やボタンを見つけられなくなる可能性がある。
- Chrome Syncの完了を直接検知するAPIがないため、同期開始時の待機は推測を含む方法である。
- 提出期限、通知、タグ、任意の並び替えは未実装である。
- 自動削除は期限の瞬間ではなく、期限経過後に対象画面を次に開いたときに行われる。

## 10. 今後の改善と追加検証

- 試用人数と期間を増やし、操作性の感想に加えて、登録数・確認回数・提出忘れ件数などを同じ条件で記録する。
- 実際のChromeとスタログ上で、通常表示、Tree Ivy、複数パソコン間の同期を含むE2Eテストを行う。
- 提出期限と期限が近いタスクの表示方法を追加し、通知が本当に必要かを検証する。
- 並び替えやタグは、入力の手軽さを損なわないUIを比較してから実装する。
- スタログのDOM変更を想定し、検出に失敗した場合に利用者へ分かりやすく知らせる。
- スマートフォン対応や独自のWebシステムを検討する場合は、学校の認証、個人情報、運用費、管理責任を先に整理する。

## 11. まとめ

スタログの授業画面へタスク管理を組み込むことで、別のメモアプリを開かずに入力と確認を行える仕組みを制作できた。少人数の試用では手軽さと分かりやすさに肯定的な意見が得られ、自動テストでは主要なデータ処理と画面更新を確認した。

一方、今回の結果だけでは忘れ物や提出忘れが減ったとは判断できない。今後は、実環境での検証と長期間・多人数の利用記録を行い、操作性だけでなく実際の効果も評価する必要がある。
